{
  "name": "ai.law",
  "version": "0.0.1",
  "description": "Law for Data Scientists",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "docs:prepare": "gitbook install",
    "docs:watch": "npm run docs:prepare && gitbook serve",
    "docs:publish1": "npm run docs:build && gh-pages -d _book",
    "docs:build": "npm run docs:prepare && rm -rf _book && gitbook build",
    "docs:publish": "npm run docs:build && cd _book && git init && git commit --allow-empty -m 'Update docs' && git checkout -b gh-pages && git add . && git commit -am 'Update docs' && git push git@github.com:passiona/ai.law gh-pages --force"
  },
  "authors": "passiona cottee, ",
  "license": "Creative Commons International 4.0",
  "devDependencies": {
    "gh-pages": "^2.1.1",
    "gitbook-cli": "^2.3.2"
  }
}
